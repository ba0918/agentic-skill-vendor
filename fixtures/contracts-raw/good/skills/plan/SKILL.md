---
name: plan
description: Turns a request into the steps the runtime will drive.
metadata:
  contracts:
    - report-format
    - workflow-runtime
---

# Plan

Run `python scripts/_runtime/runtime.py` over the steps this skill writes.
